<div class="container">
    <form action="/check" enctype="multipart/form-data" method="post">
        <div>
            <div class="input_wrapper">
                <input type="file" name="loadedFile">
            </div>
            <div class="input_wrapper">
                <input class="button" type="submit" value="Загрузить">
            </div>
            {{#if rmape}}
                <div>
                    <span>MAE</span> <div class="output">{{mae}}</div>
                </div>
                <div>
                    <span>MAPE</span> <div class="output">{{rmape}}</div>
                </div>
            {{/if}}
        </div>
    </form>
</div>
